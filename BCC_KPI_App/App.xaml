<Application x:Class="BCC_KPI_App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:BCC_KPI_App.Helpers"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Стиль для кнопок меню -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="15,0">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#34495E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Эффект тени -->
        <DropShadowEffect x:Key="ShadowEffect" 
                          BlurRadius="15" 
                          Opacity="0.1" 
                          ShadowDepth="3"
                          Color="#2C3E50"/>

        <!-- Конвертеры -->
        <helpers:UnitNamesConverter x:Key="UnitNamesConverter"/>
        <helpers:TargetValuesConverter x:Key="TargetValuesConverter"/>
        <helpers:ActualValuesConverter x:Key="ActualValuesConverter"/>
        <helpers:ProgressColorConverter x:Key="ProgressColorConverter"/>
    </Application.Resources>
</Application>