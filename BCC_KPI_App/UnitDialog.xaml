<Window x:Class="BCC_KPI_App.UnitDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подразделение" 
        Height="300" Width="400"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Название:" Margin="0,5"/>
        <TextBox Grid.Row="1" Text="{Binding Unit.UnitName}" Margin="0,5"/>

        <TextBlock Grid.Row="2" Text="Тип:" Margin="0,5"/>
        <ComboBox Grid.Row="3" 
                  SelectedItem="{Binding Unit.UnitType}"
                  Margin="0,5">
            <ComboBoxItem Content="Завод"/>
            <ComboBoxItem Content="Торговый дом"/>
        </ComboBox>

        <TextBlock Grid.Row="4" Text="Город:" Margin="0,5"/>
        <TextBox Grid.Row="5" Text="{Binding Unit.City}" Margin="0,5"/>

        <StackPanel Grid.Row="6" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="OK" 
                    Click="Ok_Click"
                    Background="#2ECC71"
                    Foreground="White"
                    Padding="20,10"
                    Margin="5"
                    Cursor="Hand"/>
            <Button Content="Отмена" 
                    Click="Cancel_Click"
                    Background="#E74C3C"
                    Foreground="White"
                    Padding="20,10"
                    Margin="5"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>