<Page x:Class="BCC_KPI_App.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:viewModels="clr-namespace:BCC_KPI_App.ViewModels"
      Title="AdminPage"
      Background="#F5F5F5">

    <Page.DataContext>
        <viewModels:AdminViewModel />
    </Page.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="⚙ Управление подразделениями" 
                   FontSize="32" 
                   FontWeight="Bold" 
                   Foreground="#2C3E50"
                   Margin="0,0,0,20"/>

        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="10" 
                Padding="15" 
                Margin="0,0,0,20"
                BorderBrush="#BDC3C7"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Padding="10"
                         BorderBrush="#BDC3C7"
                         BorderThickness="1"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="🔍 Поиск" 
                            Command="{Binding SearchCommand}"
                            Background="#3498DB"
                            Foreground="White"
                            Padding="15,10"
                            Margin="5,0"
                            Cursor="Hand"/>
                    <Button Content="➕ Добавить" 
                            Command="{Binding AddUnitCommand}"
                            Background="#2ECC71"
                            Foreground="White"
                            Padding="15,10"
                            Margin="5,0"
                            Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <ListView Grid.Row="2" 
                  ItemsSource="{Binding Units}"
                  SelectedItem="{Binding SelectedUnit}"
                  Background="White"
                  BorderThickness="1"
                  BorderBrush="#BDC3C7">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название" Width="250" DisplayMemberBinding="{Binding UnitName}"/>
                    <GridViewColumn Header="Тип" Width="150" DisplayMemberBinding="{Binding UnitType}"/>
                    <GridViewColumn Header="Статус" Width="150" DisplayMemberBinding="{Binding Status}"/>
                    <GridViewColumn Header="Город" Width="200" DisplayMemberBinding="{Binding City}"/>
                    <GridViewColumn Header="Действия" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="✏️" 
                                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            CommandParameter="{Binding}"
                                            Background="#F39C12"
                                            Foreground="White"
                                            Padding="10,5"
                                            Margin="2"
                                            Cursor="Hand"/>
                                    <Button Content="🗑️" 
                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                            CommandParameter="{Binding}"
                                            Background="#E74C3C"
                                            Foreground="White"
                                            Padding="10,5"
                                            Margin="2"
                                            Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>